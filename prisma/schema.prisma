generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Subscripbe {
  id         String @id @default(uuid())
  user_email String @unique
}
